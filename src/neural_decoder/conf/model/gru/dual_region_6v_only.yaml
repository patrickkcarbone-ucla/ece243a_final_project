# GRU for dual-region dataset using only Area 6v (channels 0-255)
# Best performing configuration for dual-region data
_target_: neural_decoder.models.gru.GRUDecoder
name: gru/dual_region_6v_only
neural_dim: 512
n_classes: 40
hidden_dim: 1024
layer_dim: 5
dropout: 0.4
strideLen: 4
kernelLen: 32
gaussianSmoothWidth: 2.0
use_channels: 6v_only

